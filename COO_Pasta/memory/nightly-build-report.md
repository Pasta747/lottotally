# Nightly Build Report — 2026-03-20 (re-run)

Generated by Einstein after watchdog missing-file alert.

## Scope
Re-ran nightly CI build checks across active products:
- Pinger
- Canopy Filter
- LottoTally

## Commands executed
1. `cd /root/PastaOS/products/pinger && npm run -s build -w apps/web`
2. `cd /root/PastaOS/products/canopy-filter/app && npm run build`
3. `cd /root/PastaOS/products/lottosync && npm run build`

## Results

### Pinger
- Build: ✅ PASS
- Route generation includes: `/robots.txt`, `/sitemap.xml`, `/status/[slug]`, checkout/dashboard/blog routes.

### Canopy Filter
- Build: ✅ PASS
- Route generation includes: `/api/billing/checkout`, `/api/digest/run`, `/api/ops/preflight`, `/dashboard`.

### LottoTally
- Build: ✅ PASS
- Route generation includes: `/calculator` plus dashboard/auth routes.
- Warning: `middleware` file convention deprecated (`middleware` → `proxy`) ⚠️ non-blocking.

## Pass/Fail summary
- Overall CI build status: ✅ GREEN (3/3 successful)
- Blocking failures: none
- Non-blocking warnings: LottoTally Next.js middleware deprecation warning

## Remaining follow-up
1. Add/track fix for LottoTally middleware→proxy migration.
2. Continue launch-day runtime checks (checkout/webhook/live monitoring) outside compile-time CI.
